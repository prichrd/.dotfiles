#!/bin/bash
set -e

BASE_PATH=$1
if [[ -z $BASE_PATH ]]; then
    exit 0
fi

selected=$(fd .git $BASE_PATH --hidden --type d | xargs dirname | fzf -q "$1")
if [[ -z $selected ]]; then
    exit 0
fi

echo $selected
